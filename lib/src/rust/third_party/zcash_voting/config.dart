// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

/// Dynamic round metadata authenticated by trusted static keys.
class AuthenticatedRound {
  final String roundId;
  final Uint8List eaPk;

  const AuthenticatedRound({required this.roundId, required this.eaPk});

  @override
  int get hashCode => roundId.hashCode ^ eaPk.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is AuthenticatedRound &&
          runtimeType == other.runtimeType &&
          roundId == other.roundId &&
          eaPk == other.eaPk;
}

/// Structured status emitted while resolving a voting config.
class ConfigCondition {
  final ConfigConditionKind kind;
  final bool status;
  final String message;

  const ConfigCondition({
    required this.kind,
    required this.status,
    required this.message,
  });

  @override
  int get hashCode => kind.hashCode ^ status.hashCode ^ message.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ConfigCondition &&
          runtimeType == other.runtimeType &&
          kind == other.kind &&
          status == other.status &&
          message == other.message;
}

/// Machine-readable condition categories for config resolution.
enum ConfigConditionKind {
  staticHashPinVerified,
  staticConfigDecoded,
  dynamicConfigDecoded,
  dynamicSignaturesVerified,
  versionsSupported,
}

/// High-level meaning of a resolved config switch.
enum ConfigSwitchKind {
  /// The resolved config summary is unchanged.
  unchanged,

  /// There was no prior resolved config summary.
  initialLoad,

  /// Same authenticated round set and protocol tuple, but endpoint or
  /// trusted-signing-key material changed.
  ///
  /// Wallets should restart endpoint caches, status polls, share tracking,
  /// and PIR/delegation precompute. They should keep round-id-indexed wallet
  /// artifacts.
  sameChainServiceUpdate,

  /// The authenticated round set changed.
  ///
  /// Wallets should treat this as moving to a new chain/round context for
  /// active UI and in-flight work. Durable state for old round ids can remain
  /// indexed by round id, but the current visible round context should be
  /// discarded or reselected from the reloaded authenticated round list.
  newChainOrRound,

  /// The resolved protocol-version tuple changed.
  ///
  /// Wallets should stop using cached voting state until compatibility is
  /// re-established.
  protocolChanged,
}

/// Resolved dynamic voting config, ready for wallet use.
class ResolvedVotingConfig {
  final String sourceFingerprint;
  final String trustedKeyFingerprint;
  final String dynamicConfigFingerprint;
  final List<ServiceEndpoint> voteServers;
  final List<ServiceEndpoint> pirEndpoints;
  final SupportedVersions supportedVersions;
  final List<AuthenticatedRound> authenticatedRounds;
  final List<String> skippedRoundIds;
  final List<ConfigCondition> conditions;

  const ResolvedVotingConfig({
    required this.sourceFingerprint,
    required this.trustedKeyFingerprint,
    required this.dynamicConfigFingerprint,
    required this.voteServers,
    required this.pirEndpoints,
    required this.supportedVersions,
    required this.authenticatedRounds,
    required this.skippedRoundIds,
    required this.conditions,
  });

  @override
  int get hashCode =>
      sourceFingerprint.hashCode ^
      trustedKeyFingerprint.hashCode ^
      dynamicConfigFingerprint.hashCode ^
      voteServers.hashCode ^
      pirEndpoints.hashCode ^
      supportedVersions.hashCode ^
      authenticatedRounds.hashCode ^
      skippedRoundIds.hashCode ^
      conditions.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ResolvedVotingConfig &&
          runtimeType == other.runtimeType &&
          sourceFingerprint == other.sourceFingerprint &&
          trustedKeyFingerprint == other.trustedKeyFingerprint &&
          dynamicConfigFingerprint == other.dynamicConfigFingerprint &&
          voteServers == other.voteServers &&
          pirEndpoints == other.pirEndpoints &&
          supportedVersions == other.supportedVersions &&
          authenticatedRounds == other.authenticatedRounds &&
          skippedRoundIds == other.skippedRoundIds &&
          conditions == other.conditions;
}

/// Endpoint advertised by a voting service config.
class ServiceEndpoint {
  final String url;
  final String label;

  const ServiceEndpoint({required this.url, required this.label});

  @override
  int get hashCode => url.hashCode ^ label.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ServiceEndpoint &&
          runtimeType == other.runtimeType &&
          url == other.url &&
          label == other.label;
}

/// Protocol component versions advertised by the dynamic config.
class SupportedVersions {
  final List<String> pir;
  final String voteProtocol;
  final String tally;
  final String voteServer;

  const SupportedVersions({
    required this.pir,
    required this.voteProtocol,
    required this.tally,
    required this.voteServer,
  });

  @override
  int get hashCode =>
      pir.hashCode ^
      voteProtocol.hashCode ^
      tally.hashCode ^
      voteServer.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SupportedVersions &&
          runtimeType == other.runtimeType &&
          pir == other.pir &&
          voteProtocol == other.voteProtocol &&
          tally == other.tally &&
          voteServer == other.voteServer;
}
